SB-Tree 数据结构深度分析
SB-Tree整体架构

两层结构：搜索层与数据层。 SB-Tree将索引结构拆分为搜索层（search layer）和数据层（data layer）两个逻辑层，以适应时序数据插入吞吐高、范围查询多的特点。搜索层是一个缓存优化的B+树，用于为数据层中的数据块提供快速查找路径。数据层则保存实际的数据记录，包含有序链接的数据块（data blocks）以及特殊的分段块（segmented block）。搜索层中的节点（称为搜索块）存储每个数据块的最小键及指针，指向数据层对应的数据块，实现类似B+树叶节点的索引功能。这种分层设计 decouples 数据组织和索引，加上异步更新搜索层，降低了写放大和插入开销。

数据层与分段块： 数据层中的数据块按键值有序链接成链表结构，每个数据块大小固定（4KB）并存储一段连续范围的键值对。在典型工作负载下，新插入的时间序列数据键往往单调递增，所有线程趋向于同时插入数据层“最右侧”的最后一个数据块中。这会导致对该叶节点的剧烈竞争。为缓解这一单点争用瓶颈，SB-Tree在数据层引入了一个特殊的分段块（Segmented Block）作为最右端的“叶节点”。分段块并不直接存储数据，而是包含多个线程私有数据块（per-thread data blocks）的指针及元信息。具体来说，每个工作线程在分段块中拥有自己独立的插入缓冲区——一个线程私有数据块，用于暂存该线程插入的记录。因此，多线程场景下，每个线程都将数据写入各自的缓冲区，避免了线程间对同一物理节点的竞争，从而极大提升并发插入性能。

Shortcut快捷指针： 为进一步减少插入路径的开销，SB-Tree设置了一个全局的**“快捷方式”指针（shortcut），直接指向数据层的当前分段块。通常，在树结构中执行插入需要从根开始逐级查找正确的叶节点。但在SB-Tree中，由于大部分新数据键都会属于当前最大的键范围，我们可以跳过常规的树遍历，直接通过shortcut定位到分段块进行插入。这样就绕过了整个搜索层的查找**过程，显著降低插入操作的延迟和CPU消耗。当分段块正在转换或切换时（详见后文），shortcut会被更新指向新的分段块，确保插入始终定位到正确的位置
researchgate.net
researchgate.net
。

数据块（Data Block）： 数据块是SB-Tree数据层中持久存在的基本存储单元，功能相当于传统B+树的叶节点。每个数据块固定大小（如4KB），内部按照分离键值存储的布局：即将所有键组成一个有序数组，所有值组成一个并行数组，这样在执行范围扫描时可以连续读取值数组而不必跳过键
researchgate.net
。这种布局利用了CPU缓存，提高了大批量读取值时的吞吐。数据块内部还利用了一种**N-ary搜索表（N-ary search table）来加速查找。具体而言，每个数据块会被划分为多个小桶（bucket），并提取每个桶中的最小键组成一个紧凑的搜索表。执行查找时，先在N-ary表中二分/顺序搜索确定目标键可能所在的桶范围，然后只对该桶内的键做线性查找，从而减少随机内存访问。相比直接在整个4KB块中二分查找，N-ary搜索表充分利用了顺序访问的预取优势，减少了缓存未命中
researchgate.net
researchgate.net
。因此，在每个数据块内，“N-ary索引 + 局部线性扫描”**取代了传统的全局二分查找，实现查询性能提升。

搜索层（Search Layer）： 搜索层维护着指向数据块的索引路径，本质上是一个B+树结构但进行了缓存优化和特殊的更新方式。SB-Tree采取自底向上构建搜索层的策略：当数据层产生新的数据块时（例如分段块完成转化，产生若干新块），将这些块的最小键依次追加到搜索层的最右侧叶节点，必要时再分裂/提升形成上层节点
researchgate.net
。由于新插入的索引键本身也是单调增加，这使得搜索树可以紧凑排列、较低高度，并避免频繁的结构修改SMO。搜索层在设计时采用了缓存友好的节点结构，并且仅有一个专门线程负责更新（插入新的索引项）。其他工作线程在查询时只读取搜索层，不涉及加锁开销。搜索层的并发控制采用读优化写排除协议（ROWEX），允许多读线程无锁访问，仅单线程执行写更新，保证读性能的同时简化了并发冲突控制。值得注意的是，搜索层的更新是异步的：并不在每次插入时实时反映数据层的新记录，而是在一定条件下批量进行（例如分段块转化后）
researchgate.net
researchgate.net
。这一架构使得插入线程将绝大部分工作集中在数据层（尤其是各自的线程数据块），大幅削减了传统B+树在更新索引目录时的同步开销。

组件关系： 综合以上，SB-Tree的整体结构如图所示（论文Fig.3）：数据层由多个4KB数据块按键序相连，最右端挂接一个分段块。分段块管理着N个线程私有数据块，每个负责暂存一个线程的新插入数据。当某线程私有块填满时，触发将整个分段块转化为若干正式的数据块加入链表，并产生新的分段块用于后续插入（详见下文流程）。搜索层维护数据块的索引路径，但由于有shortcut，全新的数据通常直接进入分段块而不经搜索层查找；只有当插入数据不属于最新序列（即延迟数据）或进行查询时，才通过搜索层定位相应数据块。下节将详细描述插入和查询的过程。

数据插入与查询的流程
顺序数据的插入路径

对于按时间顺序到达的新数据（In-order inserts）——键值比当前索引中所有键都大的情况——SB-Tree采取快捷路径插入。如上所述，插入线程首先利用shortcut直接定位到当前的分段块，而无需遍历整棵树。随后，在分段块内检查该线程是否已有分配的线程私有数据块用于写入：

线程私有块分配： 若线程首次插入且尚未拥有私有块，或之前的私有块已满，则SB-Tree通过其块分配器（后述）申请一个新的数据块，作为该线程新的插入缓冲，并将其挂接到分段块的线程块列表中。分段块维护一个原子计数来分配新的线程块指针位置。每个分段块会设定最大线程块数阈值，避免无限增长；一旦线程块数量超出阈值，分段块也会停止继续插入，转而触发转化。通常阈值可按预估并发线程数设置（例如等于最大并发线程数），保证每线程有且仅有一个活跃块。

插入写入： 如果线程已有可用的私有块且未满，则直接将新的键值对追加写入该块。线程私有块内部以**<key, value>紧邻存储**（KV Entry）的形式记录插入数据。也就是说，键和值作为一个整体元组连续地附加在内存缓冲区末尾，而不是分别存放在两个不同数组中。这种设计是为了优化插入内存访问的局部性：相比将键和值分隔存储需要两次内存访问，KV连存保证每次插入只涉及一次连续的内存写入，命中同一CPU缓存行，从而减少缓存失效，提高插入吞吐。当一个线程私有块被填满（例如4KB写满若干KV条目），它不会立即分裂自己，而是由分段块级别统一处理转换。

通过上述机制，SB-Tree将绝大多数顺序插入压力分散到各线程各自的内存区域，实现真正的无锁并行插入：线程之间几乎没有共享可写数据结构，因而不存在传统B+树叶节点上的锁争用问题。研究表明，在纯顺序插入场景下（所有键单调递增），SB-Tree可较现有索引实现最高约7倍的吞吐提升。插入操作的伪代码在论文Algorithm 1中给出，首先尝试ShortcutInsert，若返回false（表示非顺序插入），才走常规路径。

分段块转化为数据块的流程

当任一线程私有数据块写满后，当前的分段块将冻结插入并触发转化（conversion）。为了不中断插入流程，SB-Tree在转换开始前，预先分配一个新的空分段块，并将shortcut指针切换过去，让后续插入操作直接发生在新分段块上。这样，负责转换的线程可以独立地处理旧分段块的数据，而其他线程仍可在新分段块上继续各自的插入，做到插入与转换并行，提升并发度。

转换线程接下来执行分段块合并排序过程，将旧分段块内所有线程私有数据块的内容汇总并生成有序的数据块：

收集合并： 转换线程首先读取该分段块下属的所有线程私有块，将其中存储的KV条目全部取出合并到一个临时列表中。由于各线程插入的数据本身按各自先后顺序基本有序，并且全局上键是单调增加的，因此来自不同线程块的数据大致已按键排序，只是交织在一起
researchgate.net
。这意味着合并后的全集在排序时需要移动的元素较少，排序开销相对有限
researchgate.net
。实验证明，多线程的KV片段由于时间序列特性通常“部分有序”，排序成本不会成为瓶颈
researchgate.net
。

排序： SB-Tree使用C++标准库的std::sort对合并后的KV列表按键值升序排序
researchgate.net
。排序确保后续生成的数据块严格按照键递增，符合B+树叶节点的要求。如果延迟数据导致某些键稍小于整体序列，也会在此阶段正确排序。

切分写入数据块： 将排好序的所有KV条目顺次切分装入若干新的4KB数据块中
researchgate.net
。这些新数据块继承SB-Tree数据块的布局，将键和值分别存储在独立的数组中，以优化后续范围查询性能
researchgate.net
。每个数据块的最大容量一般取决于4KB能容纳的KV数量。转换完成后，这些数据块按键序连接到数据层链表中原分段块的位置。例如，原先分段块处于某前驱数据块之后、某后继节点（可能为空）之前，那么新的多个数据块将链接在前驱之后、后继之前，完整地替代掉原分段块在链表中的位置。

更新元数据： 转换产生的新数据块需要被搜索层索引到：转换线程会将每个新数据块的最小键和指针记录下来，稍后由搜索层更新线程插入索引（详见下节）
researchgate.net
researchgate.net
。同时，原分段块及其中线程私有块占用的内存可以释放或重用。SB-Tree采用自定义的块内存分配器来高效回收这些块，以便后续再次利用（见“内存管理”部分）。

整个转换过程中，SB-Tree通过版本锁机制保证数据一致性：在转换开始时，对将转换的分段块加一个版本锁，阻止其他线程再往该分段块及其线程块写入新数据。因为我们已经把shortcut转向新块，所以正常插入流程不会再访问旧分段块；版本锁仅用于防止极少数延迟插入线程误入。转换完成后释放锁。这样确保转换期间旧分段块的数据视图是稳定不变的。

转换完成后，新生成的多个数据块成为数据层最新的“尾部”。由于我们提前准备了新分段块，后续插入已经在新分段块继续，不会丢失任何数据。值得一提的是，在一次转换过程中，大量数据块和内存需要分配释放，SB-Tree通过块分配器批量处理这些内存操作，将系统调用和同步开销降至最低。在1亿级别插入实验中，80线程环境下SB-Tree总共触发了约36,646次分段块转换（平均每线程458次），仍能维持插入性能的线性提升。

延迟数据插入的处理路径

延迟数据是指乱序到达的旧时间戳数据，即键值比当前最新插入的键要小的数据
researchgate.net
researchgate.net
。这些数据无法通过shortcut直接插入，SB-Tree将其归类为“Delayed”并采用常规B+树插入路径处理
researchgate.net
。具体过程如下：

搜索层查找： 插入线程对延迟数据首先进行搜索层B+树遍历，找到目标键所属的叶节点或邻近叶节点
researchgate.net
。由于搜索层可能略滞后于数据层最新状态（异步更新），找到的叶节点有可能是邻近节点而非精确位置
researchgate.net
。例如，假设延迟数据键应该插入在数据块X，但搜索层尚未包含X（X可能是刚由转换产生尚未索引的块），则搜索可能定位到X的前驱块W。

数据层校验： 为确保准确，SB-Tree在获得搜索层结果后，会顺着数据层链表从该结果块开始前后检查，确认目标键实际应该插入的块位置
researchgate.net
researchgate.net
。一般通过比较相邻块的键范围（论文中函数Covers(block.nextBlock, key)表示下个块是否“覆盖”目标键）来判断是否需要移动到相邻块
researchgate.net
researchgate.net
。最终可以精确定位出需要插入的真正数据块。

插入与分裂： 找到目标数据块后，将延迟数据的键值对插入该块的适当位置，保持块内键的有序性
researchgate.net
。如果该数据块尚有空间（未满4KB），插入后更新块内键值计数即可；如果已满，则按照一般B+树分裂规则将该数据块一分为二（约一半的键移到新块），并在数据层链表中相邻插入新块
researchgate.net
。新块的最小键也会记录下来以供搜索层稍后更新索引。此外，对于延迟插入操作，需要对目标数据块加版本锁（或类似锁)来序列化并发插入，防止多个线程同时插入同一块造成不一致。SB-Tree在数据块级别采用了一种基于版本号的锁协议来处理这种并发，保证延迟插入的安全。

通过上述处理，延迟数据被正确插入到相应位置，SB-Tree保证整个数据层仍按全局排序。延迟插入相对于顺序插入开销更高，一方面因为需要遍历树和可能的块分裂，另一方面也因为涉及锁机制。因此，如果延迟数据比例较大时，SB-Tree的性能会退化接近传统B+树。不过作者在评测中设计了延迟数据混合的负载，SB-Tree仍展示出相当的性能优势。总体而言，SB-Tree对少量乱序数据是健壮的，但不鼓励极高比例的乱序写入（详见后文局限性）。

点查询（Lookup）和范围扫描（Scan）的执行路径

点查询： SB-Tree的读操作首先通过搜索层快速定位目标所在的数据块，然后在块内利用N-ary搜索表和线性查找获取值。具体来说，查找(key)时，先在搜索层B+树中定位到key所属的叶节点（可能直接指向某数据块或其邻近块）。如果搜索层索引稍有滞后，同插入情况，算法会检查相邻块（通过Covers()循环）以确保定位正确块。找到正确的数据块后，在块内先扫描N-ary搜索表：该表包含每个bucket的最小键，大小远小于整块，可以很快在缓存中扫描完
researchgate.net
researchgate.net
。通过比较key确定其应属于哪个bucket，然后在对应bucket中进行线性顺序扫描，查找匹配的key
researchgate.net
researchgate.net
。由于bucket规模相对较小且连续内存布局，此过程对CPU缓存友好。如果找到key，则返回对应值；如未找到且key大于当前块最大键，则需到链表下一个数据块继续查找（不过搜索层通常已保证我们在正确范围起点）。借助N-ary表，SB-Tree查找时的随机存取显著减少，大部分内存访问都是顺序的，从而提升查询速度。相比Blink-Hash等使用哈希表查找的结构，SB-Tree点查找需要遍历搜索树和块内线性搜索，性能略逊一些，但仍维持在与其他B+树变种相当的水平。

范围扫描： SB-Tree非常适合范围查询（scan），其流程与点查询类似，但会连续读取多个数据块。扫描操作给定起始key和需要读取的count（条目数）。首先，通过搜索层定位起始key所在的数据块（同样用Covers检查邻居确保准确）。然后在起始块内利用N-ary表找到起始key所在bucket，线性扫描该bucket获取从起始key开始的记录序列。获取值时，由于数据块将值集中存放，CPU每次内存访问可以读取一连串值，在长扫描中预取效率极高。扫描会将该块中后续记录依次加入结果，直到达到所需数量或该块结束。如果还未收集够count条记录且有下一个数据块，则移动到下一个块继续扫描，重复上述过程。这样，SB-Tree的链式叶节点设计保证了范围查询可以在内存中顺序地批量读取，尤其适合时序数据的时间范围检索。作者实验显示，SB-Tree在扫描性能上相对于其他索引有明显优势：数据块较大且连续存储，使预取和缓存利用率更高，另外数据块分离键/值设计也减少了不必要的内存访存。在99.99%尾延迟上，SB-Tree的scan比同类索引低2.4倍，表现更稳定。总的来说，SB-Tree的查询方案充分利用了其分段结构和缓存优化，既保证了点查找的可接受性能，又极大提升了范围扫描的吞吐和稳定性。

同步机制与并发性

SB-Tree在架构上通过分层与分段设计，将不同类型的操作交给不同线程或模块处理，从而减少锁竞争并提高并行度。

搜索层的并发控制： 如前所述，搜索层仅由单个专职线程进行更新插入，其它线程均只执行只读访问。这采用了ROWEX（Read-Optimized Write EXclusion）并发协议，即允许任意多个读线程无锁地并发遍历树，而写操作（插入新的索引项）由一个线程排他进行，避免读写冲突。具体实现中，可能通过在更新索引节点时使用版本号或写锁来使写入原子可见，并维护一个原子计数器记录节点中的键数等元数据，以便读线程总能看到一个一致的状态
researchgate.net
researchgate.net
。由于搜索层插入并不频繁（只有当分段块转化出新数据块时才批量发生），这种单线程更新模式不会成为瓶颈，反而简化了锁机制并保障读性能。对于读线程而言，完全无锁遍历搜索B+树，不会因为并发插入而阻塞；插入线程在更新节点时一次性完成对该节点的所有修改（如插入新键指针并更新键数），保证读线程要么看到插入前状态，要么看到插入后完整状态，不会看到中间不一致结果
researchgate.net
researchgate.net
。

数据层的并发插入： 数据层通过线程私有数据块彻底消除了热点叶节点上的并发冲突。每个线程写入各自的数据块，无需加锁，即使80个线程同时插入，互不干扰。只有在处理延迟数据时，因多个线程可能定位到同一已有数据块插入，需要用锁来协调。SB-Tree采用了基于版本的锁协议（如类似乐观并发控制）：每个数据块维护一个版本号/锁。当线程要插入延迟数据到某块时，先检查该块版本是否变化，以判断有无并发写；如无则“乐观”地进行插入，完成后再增加版本号。如果期间版本号变化则重试。这种机制允许多线程并发查找不同块且尽量避免锁操作，仅在真正同块竞争时才串行化。因此SB-Tree在数据层的延迟插入也做到了细粒度锁定，降低冲突范围。对于分段块转换过程，SB-Tree在触发转换时对整个旧分段块使用版本锁将其标记为不可写。由于shortcut已经切走，新插入都不会再访问该分段块，所以该锁几乎不会被等待，只是防止极端情况下还有线程误判地投入老块。转换完成后释放锁。与此同时，新分段块上的插入照常进行，没有任何停顿。这种设计使得即便有线程在执行较耗时的分段块合并转换，其它线程的插入也无需等待，系统整体插入性能几乎不受单次转换影响。

查询并发： 查询操作都是只读的，对于搜索层和数据层都不加锁。搜索层读如上所述依赖ROWEX确保一致性，无锁遍历即可。数据层查询（点查或扫描）过程中，如果正遇到有转换在进行，也不会影响读取正确性：假如查询来到一个正在转换的旧分段块，版本锁会标识其不可用，查询线程可以沿数据链表跳过该节点，直接访问转换后新生成的数据块；或者实现上简单的方法是在转换开始时先在链表中插入新数据块并调整邻接指针，再进行旧块合并，这样查询总能找到新块，从而转换对读操作完全透明。实际上，作者指出读线程即使遇到正在添加的新数据（例如搜索层的节点键数在更新），由于写线程最后一步才原子更新计数，使读线程不会察觉中间状态
researchgate.net
。因此SB-Tree的查询和插入/转换可以高度并行，各不影响。

分配器并发： SB-Tree的内存块分配器也考虑了并发效率。每个线程维护独立的空闲块池，需要内存时从自己池中获取，不需要全局锁。当线程私有池耗尽时，再一次性从系统申请一大块内存切分补充。释放块时也先回收到线程自己的空闲池，只有在特殊情况下才会涉及全局（例如返回系统或别的线程用）。因此，在内存管理层面也避免了频繁的锁开销。

综上，SB-Tree通过单写多读的搜索层、无共享写入的数据层、版本锁协调少量冲突，再配合内存分配优化，实现了高度并行的操作。在插入-only情景下，除非出现延迟数据或分段转换，线程几乎各做各的，扩展性非常好。而混合工作负载下，查询和插入在不同层面上解耦进行，也保持了稳定的性能和低尾延迟。

内存管理策略

SB-Tree使用专门的块分配器（block allocator）来管理内存，以提高分配/释放效率和内存复用率。考虑到SB-Tree中的主要内存单元（数据块、线程块、分段块、搜索层节点等）大小相同或预定义固定，块分配器会预先向操作系统申请一大块连续内存，然后划分成等大小的块供SB-Tree使用。具体策略包括：

统一的固定大小分配： SB-Tree各类块的尺寸经过设计基本一致（例如都为4KB），这样一个空闲块可以通用地用于数据层或搜索层的任何节点。当某处释放一个块内存后，可以直接加入空闲池，将来为其它用途重复使用，而无须返还系统或另行初始化。这避免了不同结构间内存碎片和不兼容问题，提高了内存利用率。

批量分配与回收： 块分配器在需要时会一次性分配一批块放入自由列表，而不是频繁地逐块向系统申请
researchgate.net
researchgate.net
。比如启动时可能分配N个4KB块缓冲区。这样可以将系统调用次数降到很低， amortize 初始化成本。在批量回收时也可考虑延迟释放给操作系统，从而减少系统调用抖动。

线程局部空闲池： 为减少多线程争抢内存分配锁，块分配器让每个线程维护自己的空闲块列表。线程在插入需要新块（如分段块分配线程私有块，或数据块分裂，新索引块生成）时，首先从本线程的空闲池拿。如果自己池空了，再从全局池获取一批。如果全局也空则向OS申请。一旦线程释放某块（如数据块被淘汰），则优先回收到本线程池
researchgate.net
researchgate.net
。这种设计下，大部分内存申请和释放都在线程内完成，无锁操作，不会造成多线程竞争。

重复利用与减少碎片： 由于数据块生命周期可能较短（比如线程私有块用完就转正为数据块或释放），块分配器高度复用内存。尤其转换过程中，会释放掉分段块和线程块，将它们立即加入空闲池用于后续新块创建，形成内存循环再利用
researchgate.net
。这样在长时间运行下，也不会产生大量内存碎片或膨胀。作者提到，通过块分配器的复用，SB-Tree在高并发下内存分配的系统调用开销显著低于直接使用标准malloc/free或TCMalloc等。

总之，块分配器为SB-Tree提供了高速的内存管理支持，在插入密集型负载下避免了动态分配的瓶颈。实验证明，相比不使用块分配器的情况，SB-Tree因减少系统调用和锁开销而获得了更高的插入吞吐。同时，由于所有块大小统一，结构内存占用相对紧凑，没有因为复杂分配而导致显著的空间浪费。这样的内存管理策略非常适合内存数据库的使用模式。